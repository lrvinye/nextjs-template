// 第一个语言是默认语言
const SUPPORTED_LANGUAGES = ["en", "zh"];
// 第一个是默认命名空间
const NAMESPACES = ["common"];

module.exports = {
  SUPPORTED_LANGUAGES,
  NAMESPACES,
  debug: false,
  defaultNS: NAMESPACES[0],
};
